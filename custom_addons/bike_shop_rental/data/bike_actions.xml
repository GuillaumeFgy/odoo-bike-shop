<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Server Action pour assigner les images en masse -->
    <record id="action_bike_assign_images_bulk" model="ir.actions.server">
        <field name="name">Assigner Images Automatiques</field>
        <field name="model_id" ref="model_bike_bike"/>
        <field name="binding_model_id" ref="model_bike_bike"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    records.action_assign_default_image()
        </field>
    </record>
</odoo>
